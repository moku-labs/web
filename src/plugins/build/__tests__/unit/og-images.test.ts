import { describe, it } from "vitest";

describe("build/phases/og-images", () => {
  it.todo("skips unchanged articles via content-hash cache");
  it.todo("bounds concurrency to <=4 via p-limit");
  it.todo("surfaces the font-validation error path");
});
