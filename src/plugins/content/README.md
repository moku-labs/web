# content

> Complex plugin — Markdown content pipeline: discover, parse frontmatter, render to sanitized HTML, and expose a locale-keyed Article model. Depends on i18n.

## API

<!-- Populated during build -->

## Configuration

<!-- Populated during build -->
