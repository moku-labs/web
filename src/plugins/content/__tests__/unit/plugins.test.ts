import { describe, it } from "vitest";

describe("content/pipeline/plugins", () => {
  it.todo("default remark plugins include parse, frontmatter, gfm, directive");
  it.todo("default rehype plugins include rehype-raw and the custom transforms");
});
