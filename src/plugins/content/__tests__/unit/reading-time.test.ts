import { describe, it } from "vitest";

describe("content/pipeline/reading-time", () => {
  it.todo("rounds minutes up (ceiling)");
  it.todo("enforces a minimum of 1 minute");
  it.todo("returns the source word count");
});
