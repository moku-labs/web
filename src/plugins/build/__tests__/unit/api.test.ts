import { describe, it } from "vitest";

describe("build/api", () => {
  it.todo("phases() returns the static order");
  it.todo("run({ outDir }) override is honored");
  it.todo("validateConfig throws actionable [web] build.outDir on empty outDir");
  it.todo("validateConfig throws when ogImage enabled but fontDir has no fonts");
});
