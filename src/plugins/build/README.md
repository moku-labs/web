# build

> Complex plugin — SSG orchestrator: sequences the multi-phase pipeline (clean, bundle, content/images, pages, feeds/sitemap/og-images, root-index) and writes the site to `dist/`.

## API

<!-- Populated during build -->

## Configuration

<!-- Populated during build -->
