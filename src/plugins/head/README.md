# head

> Standard plugin — composes the document `<head>`: title, meta, OG/Twitter cards, canonical + hreflang, JSON-LD, feeds. Pulled synchronously by `build` via `ctx.require`.

## API

<!-- Populated during build -->

## Configuration

<!-- Populated during build -->
