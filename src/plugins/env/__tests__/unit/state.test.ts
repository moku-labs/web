import { describe, it } from "vitest";

describe("env/state", () => {
  it.todo("createEnvState returns empty resolved and publicMap maps");
  it.todo("each createEnvState call returns independent maps");
});
