import { describe, it } from "vitest";

describe("content plugin integration", () => {
  it.todo("createApp + fixtures: loadAll returns a locale-keyed map with expected counts");
  it.todo("emits content:ready (assert via log harness)");
  it.todo("byte-compares rendered Article.html against the committed golden fixture");
  it.todo("verifies locale fallback and production draft filtering end-to-end");
});
