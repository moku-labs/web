import { describe, it } from "vitest";

describe("build/phases/bundle", () => {
  it.todo("invokes Bun.build with correct entrypoints + minify flag");
  it.todo("caches hashed asset paths in state.buildCache");
});
