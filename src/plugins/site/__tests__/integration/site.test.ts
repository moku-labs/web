import { describe, it } from "vitest";

describe("site integration", () => {
  it.todo("createApp with site config constructs and exposes app.site.* values end-to-end");
  it.todo("createApp({}) throws fail-fast with [web] site.name is required.");
  it.todo("ctx.require(sitePlugin) observes the same frozen values cross-plugin");
});
