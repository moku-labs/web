import { describe, it } from "vitest";

describe("spa kernel", () => {
  it.todo("processNav swaps the region, updates currentUrl, emits navigate then navigated");
  it.todo("double-boot guard: booting twice throws the Part-3 already-started error");
  it.todo("resolveSpaConfig applies defaults and rejects invalid swapSelector");
});
