import { describe, it } from "vitest";

describe("spa router", () => {
  it.todo("scroll restoration restores prior position on back/forward and resets on new nav");
  it.todo("falls back to full browser navigation on fetch error");
  it.todo("wraps swap in startViewTransition when enabled and supported");
});
