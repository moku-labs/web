import { describe, it } from "vitest";

describe("deploy generators integration", () => {
  it.todo("snapshots wrangler.jsonc from slug + outDir + compatibilityDate");
  it.todo("snapshots deploy.yml with SHA-pinned actions");
  it.todo("emits a single MOKU_WRANGLER_VERSION source of truth in deploy.yml");
});
