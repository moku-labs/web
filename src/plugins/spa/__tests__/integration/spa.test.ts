import { describe, it } from "vitest";

describe("spa integration", () => {
  it.todo("createApp with router + head + spa: start then navigate then stop full cycle");
  it.todo("spa:navigate and spa:navigated fire with correct payloads via log.expect");
  it.todo("onStop removes listeners: a post-stop simulated link click does nothing");
});
