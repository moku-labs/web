import { describe, it } from "vitest";

describe("i18n integration", () => {
  it.todo("createApp constructs synchronously with a full i18n config");
  it.todo("app.i18n.* returns expected values through the wired surface");
  it.todo('createApp({}) constructs with defaults and defaultLocale() === "en"');
  it.todo("createApp throws fast for an invalid i18n config");
});
