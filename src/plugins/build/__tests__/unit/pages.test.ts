import { describe, it } from "vitest";

describe("build/phases/pages", () => {
  it.todo("pulls router.manifest() + head.render() for each route");
  it.todo("injects build-id meta tag into rendered pages");
  it.todo("writes correct output paths");
});
