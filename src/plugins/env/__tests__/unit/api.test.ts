import { describe, it } from "vitest";

describe("env/api", () => {
  it.todo("get returns the resolved value or undefined");
  it.todo("require returns the value or throws when undefined");
  it.todo("has reports presence of a resolved variable");
  it.todo("getPublic returns a frozen plain object of public variables");
  it.todo("getPublicMap returns the frozen ReadonlyMap of public variables");
});
