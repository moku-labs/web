# log

> Standard core plugin — structured logging + in-memory trace with an `expect()` DSL for LLM-verifiable workflow assertions. Injected as `ctx.log` / `app.log`.

## API

<!-- Populated during build -->

## Configuration

<!-- Populated during build -->
